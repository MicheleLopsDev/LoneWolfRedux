syntax = "proto3";

option java_package = "io.github.luposolitario.lonewolfredux.datastore";
option java_multiple_files = true;

message TranslationCache {
  // Usiamo una map dove la chiave è l'hash del testo originale
  // e il valore è il testo tradotto.
  map<int64, string> translations = 1;
}